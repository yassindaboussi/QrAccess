package app.tn.qraccess.presentation.theme

import androidx.compose.ui.graphics.Color

// ── Primary brand — Electric Teal ──────────────────────────
val Primary        = Color(0xFF00C9A7)   // electric teal
val PrimaryLight   = Color(0xFF4DDBBE)
val PrimaryDark    = Color(0xFF00A688)
val PrimaryGlow    = Color(0x2600C9A7)   // 15% teal for glow/tint

// ── Surfaces — Deep Navy ───────────────────────────────────
val Background     = Color(0xFF0D1B2A)   // very deep navy
val Surface        = Color(0xFF152535)   // slightly lighter panel
val SurfaceCard    = Color(0xFF1C2F42)   // card surface
val SurfaceElevated= Color(0xFF243549)   // elevated card

// ── Semantic — Success ─────────────────────────────────────
val Success        = Color(0xFF00C9A7)   // same as primary for granted
val SuccessLight   = Color(0xFF4DDBBE)
val SuccessBg      = Color(0xFF0D2E28)   // dark teal tint bg
val SuccessBorder  = Color(0xFF1A5449)

// ── Semantic — Error/Denied ────────────────────────────────
val Error          = Color(0xFFFF6B6B)   // warm coral — easy on eyes
val ErrorLight     = Color(0xFFFF9494)
val ErrorDark      = Color(0xFFE84040)
val ErrorBg        = Color(0xFF2E1515)   // dark coral tint bg
val ErrorBorder    = Color(0xFF5C2020)

// ── Semantic — Warning ─────────────────────────────────────
val Warning        = Color(0xFFFFC947)
val WarningBg      = Color(0xFF2E2410)

// ── Text ──────────────────────────────────────────────────
val TextPrimary    = Color(0xFFECF0F5)   // near white, not harsh
val TextSecondary  = Color(0xFF8BA3B8)   // muted blue-gray
val TextTertiary   = Color(0xFF4E6579)   // very muted
val TextOnAccent   = Color(0xFF0D1B2A)   // dark on teal buttons

// ── Borders / Dividers ────────────────────────────────────
val Border         = Color(0xFF1E3448)
val Divider        = Color(0xFF162840)

// ── Navigation bar ────────────────────────────────────────
val NavBar         = Color(0xFF101F2E)
val NavBarSelected = Color(0xFF00C9A7)
val NavBarUnsel    = Color(0xFF4E6579)